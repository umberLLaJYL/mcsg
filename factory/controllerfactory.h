#if !defined(_CONTROLLERFACTORY_H)
#define _CONTROLLERFACTORY_H

#include <map>
#include <string>

class _ControllerFactory {
private:
   static std::map<std::string, > controllerBase;

public:
    _ControllerFactory(/* args */) { }
    ~_ControllerFactory() { }
};

#endif // _CONTROLLERFACTORY_H
